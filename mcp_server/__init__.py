"""hermes-botcoin-mcp — MCP server wrapping BOTCOIN mining tools.

Distributed as a separate PyPI package so any MCP-aware client (Hermes,
Claude Desktop, Cursor, custom agents) can use BOTCOIN by adding two lines
to its ``mcp_servers`` config — no plugin install required.
"""

__version__ = "0.1.0"
